{
    "buildCommand": "npm run build",
    "outputDirectory": "dist",
    "rewrites": [
          { "source": "/(.*)", "destination": "/api/serve" }
    ],
    "functions": {
          "api/serve.js": {
                  "includeFiles": "dist/**"
          }
    }
}
