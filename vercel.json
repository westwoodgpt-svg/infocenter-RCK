{
      "buildCommand": "npm run build",
      "outputDirectory": "dist",
      "routes": [
              { "src": "/(.*)", "dest": "/api/serve" }
      ],
      "functions": {
              "api/serve.js": {
                        "includeFiles": "dist/**"
              }
      }
}
